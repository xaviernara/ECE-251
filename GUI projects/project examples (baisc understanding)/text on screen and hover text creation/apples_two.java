import javax.swing.JFrame;

public class apples_two {
    public static void main(String[] args) {
    	tuna bucky=new tuna();
    	//setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE); IS USED FOR when you hit the (x) button the panel closes
    	bucky.setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE);
		//setSize() is used to set the size of the panel length and width wise
    	bucky.setSize(275,180);

    	//setVisible (true) is used to maked the panel visible
    	bucky.setVisible(true);
    }
}
